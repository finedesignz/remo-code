{
  "name": "@remo-code/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing a remo-code hub's external session-ask API (read a session's transcript/memory for free; ask it a question when the free reads are inconclusive).",
  "bin": {
    "remo-code-mcp": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  }
}
