{
  "_comment": "Snapshot of expected hub test counts. Updated when intentionally adding/removing tests. CI fails if actual drifts beyond tolerance.",
  "captured_at": "2026-05-28",
  "captured_from": "per-file-isolated `bun run check-baseline` (each hub/test file in its own process — order-independent, local==CI). Prior 715/836 predated #157 + the B-series obs tests; bumped to true green. fail_max stays 0.",
  "pass": 771,
  "skip": 129,
  "fail": 0,
  "total": 900,
  "tolerance": {
    "_comment": "Allow modest growth without churn; any fail > 0 always fails CI.",
    "pass_min": 700,
    "skip_max": 150,
    "fail_max": 0
  }
}
