{
  "_comment": "Snapshot of expected hub test counts. Updated when intentionally adding/removing tests. CI fails if actual drifts beyond tolerance.",
  "captured_at": "2026-05-28",
  "captured_from": "bun test (hub/) on main @ 5d42e0d — recaptured true green counts. Prior 715/836 predated the self-capture mock-pollution fix (#157) + several B-series obs test additions; the full suite now loads clean (no leaked partial mocks) so the recorded snapshot is bumped to match reality. fail_max stays 0.",
  "pass": 771,
  "skip": 129,
  "fail": 0,
  "total": 900,
  "tolerance": {
    "_comment": "Allow modest growth without churn; any fail > 0 always fails CI.",
    "pass_min": 700,
    "skip_max": 150,
    "fail_max": 0
  }
}
