export default function FoldersPage() {
  return (
    <div className="max-w-2xl space-y-5">
      <header>
        <h1 className="text-lg font-semibold">Allowed Folders</h1>
        <p className="text-sm text-[var(--text-muted)]">Coming soon.</p>
      </header>
      <section className="bg-[var(--bg-secondary)]/60 rounded-xl p-5">
        <p className="text-sm text-[var(--text-secondary)]">
          The folder picker and the allow-list editor will land in Wave 3.
        </p>
      </section>
    </div>
  );
}
