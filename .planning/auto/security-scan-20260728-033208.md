# Security Scan — feat/ptycap-token-gate — 2026-07-28T03:32:08Z

**Header note:** The hub injected empty template vars for repo/branch/mode into this
chain step. The canonical checkout (`C:/Users/artic/GitHub/remo-code`) is on a detached
HEAD (`acab4e5`) shared by other active sessions, so all scanning below ran in the
dedicated worktree `C:/Users/artic/GitHub/remo-code-ptycap` on branch
`feat/ptycap-token-gate`, diffed against `origin/main` (`23b86e9`). The canonical
checkout was never touched.

**Scope:** diff `origin/main...HEAD` (6 source files + 5 test files, 15 files changed,
633 insertions / 267 deletions — coolify-webhook triage log fetch, post-run chained
BLOCKED/FAILED notify, run-snippet Summary-line extraction, and the triage sender's
supervisor-spawn-path removal).

## Tooling used

- **security-review skill**: invoked via `Skill` tool. Its git-context preprocessing
  ran against the canonical repo's shell context (`HEAD detached at acab4e5`, empty
  diff) instead of this worktree — a template/harness artifact, not a scan failure.
  Compensated with a manual security read of the same diff (`git diff
  origin/main...HEAD`) covering all 6 modified source files. See Findings below.
- **Dependency scan**: `bun audit` (bun v1.3.14) at worktree root. `bun audit` exists
  as a subcommand — no npm fallback needed.
- **cargo-audit**: not installed — skipped per instructions (no install attempted).
- **Secret scan**: ripgrep across the worktree (excluding `.env*`, `node_modules`,
  `target/`, `dist/`, `.git`, lockfiles) for `AKIA`, `sk_live_`, `sk-ant-`, `ghp_`,
  `gho_`, `xoxb-`, PEM private-key headers, `olx_`, `remo_`, and bearer-token-shaped
  literals.

## Critical

None.

## High

None. `bun audit` reports 7 HIGH-severity advisories, but they are all **transitive
dependency CVEs** (postcss sourceMappingURL path traversal, vite dev-server arbitrary
file read, fast-uri host-confusion, hono CORS wildcard-with-credentials, picomatch
ReDoS) — not something the diff introduced, and dependency CVEs are tracked separately
per this chain's own scope (fix step handles dependency bumps as its own class, not
this scanner's finding).

## Medium

None found in the diff. The one new untrusted-data path this diff adds —
`fetchTriageLogSnippet` in `hub/src/api/coolify-webhook.ts:60-72` populating
`log_snippet` with real (attacker-influenceable) Coolify deploy-log text that used to
be `''` — was checked against `hub/src/scheduler/triage-prompt.ts:22,38`, which passes
`log_snippet` through the existing `fenceUntrusted()` helper before it reaches the
model prompt (same pattern documented in CLAUDE.md's "untrusted inbound payloads are
FENCED" invariant). No new injection surface.

## Low / Info

- `hub/src/scheduler/senders/triage.ts` (whole-file rewrite): removes the
  supervisor-spawn triage path (`pending` map, `onTriageAssistantMessage`,
  `triageActiveForSession`, the `setInterval` timeout sweep) entirely, per its own
  header comment documenting the prod bug it fixes (unreachable waiter keyed by the
  wrong id + a GitHub-slug passed where a local worktree path was expected). This is a
  correctness/reliability fix, not a security regression — the deleted path also
  removes the machine-triggered `dangerously_skip_permissions: false` enforcement
  block for that (now-dead) code path, but since the whole branch is deleted there is
  no live call site left where that enforcement could be needed. No action required.
- `hub/scripts/*.ts` and `hub/src/observability/*.ts` matched the `remo_` secret-scan
  pattern only as metric-name / user-id-field prefixes (`remo_orchestrator_*`,
  `remo_code_user_id`) — not credentials.
- `supervisor/test/oauth-poll-credit-absent.test.ts:15` and
  `supervisor/test/oauth-poll-dual-bucket.test.ts:19` contain strings matching the
  `sk-ant-` pattern, but both are explicitly named `FAKE_TOKEN` test fixtures
  ("FAKE-TOKEN-absent-case", "FAKE-TOKEN-must-never-leak") — not real committed
  secrets.

## Dependency scan detail (bun audit)

35 advisories total (7 high, 26 moderate, 2 low), all transitive, all pre-existing
(not introduced by this diff): `@hono/node-server`, `postcss`, `vite`/`@vitejs/plugin-react`,
`@babel/core`, `fast-uri` (via `@modelcontextprotocol/sdk`), `hono` (24 advisories on
the hono workspace dep chain), `picomatch` (via vite). No action taken here per
scope (triage is a later step in this chain).

## cargo-audit

Not installed — skipped (`cargo audit` command not found; no `audit` subcommand
present, only `cargo add`). No cargo-audit binary was installed to satisfy this scan,
per instructions.
