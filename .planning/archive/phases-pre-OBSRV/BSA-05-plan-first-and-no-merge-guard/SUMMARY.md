# BSA-05-plan-first-and-no-merge-guard — SUMMARY

Guard test: dev macro is plan-first (stops at PR), and the autospawn seam structurally cannot trigger merge-to-main (off-hours window path only; macro-cycle/inject don't import the merge primitive). Tests: orchestrator-autospawn-no-automerge.guard.test.ts.

All OFF-by-default + inert until owner arms (allowlist + REMO_ORCHESTRATOR_AUTOSPAWN flip). Cost cap untouched; no API key on human PTY path.
