---
phase: 08-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - web/src/**/*.tsx
  - web/src/lib/task-templates.ts
  - web/src/components/ui/Toggle.tsx
  - web/src/components/ui/Tabs.tsx
  - web/src/components/ui/StatusPill.tsx
  - web/src/components/ui/HeaderRight.tsx
  - web/src/components/ui/AppShell.tsx
  - web/test/no-indigo.test.ts
autonomous: true
requirements: [R-DS-01]
must_haves:
  truths:
    - "Zero occurrences of the string `indigo` anywhere under web/src"
    - "All migrated accents use blue (blue-600/500 solid, blue-500/30 rings, blue-300/400 links)"
    - "A CI test fails the build if any `indigo` reappears in web/src"
  artifacts:
    - path: "web/test/no-indigo.test.ts"
      provides: "CI grep guard that fails on any indigo occurrence in web/src"
      contains: "indigo"
  key_links:
    - from: "web/test/no-indigo.test.ts"
      to: "web/src"
      via: "recursive scan asserting 0 matches"
      pattern: "indigo"
---

<objective>
Migrate every remaining `indigo` accent in web/src to blue across all ~37 call-site files + the four ui primitives not touched in 08-01 (Toggle, Tabs, StatusPill, HeaderRight, AppShell), then add a CI guard test that fails on any future `indigo`.

Purpose: completes R-DS-01 — zero indigo in web/src, blue is the single app accent.
Output: bulk accent migration + permanent regression guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md

Accent rule (design-preferences.md, LOCKED): blue is the app accent. Mapping:
- `bg-indigo-600` → `bg-blue-600`, `hover:bg-indigo-500` → `hover:bg-blue-500`
- `text-indigo-300/400` (links/active) → `text-blue-300/400`
- `ring-indigo-500/30` → `ring-blue-500/30`, `focus:ring-indigo-500` → `focus:ring-blue-500`
- `bg-indigo-600/20` (active state) → `bg-blue-600/20`
- any other `*-indigo-*` shade → same shade under `blue`
Orange stays CTA-only; NEVER reintroduce indigo. This is a mechanical shade swap — no behavior/layout change.

Scope: 08-01 already cleared Button, Card, Field, Brand, InfoTip, index.ts. This plan covers everything else. As of planning, `grep -rc indigo web/src` reported indigo across these files (counts will be the acceptance target → 0):
  pages: Login, AuthCallback, tasks/ActivityTab, settings/{ConnectionsTab,ProfileTab,UsageTab,OrchestratorTab,PromptsTab,CredentialsTab}
  lib: task-templates.ts
  components: ConnectModal, ApiKeyModal, MessageBubble, AppErrorBoundary, QuestionBlock, CommandsList, ActivityFeed, ClaudeUsageCard, CloneHereModal, MobileAccordionShowcase, PostRunActionsEditor, ErrorBoundary, GridPage, ChatSurface, CreateGithubRepoModal, ScheduleRunsDrawer, MobileAccordionRow, CronBuilder, ScheduleRulesBuilder, SessionPicker, Sidebar, SchedulesPage, SetupForm, SupervisorPage, SessionDropdown, UnreadBadge
  ui (not in 08-01): Toggle, Tabs, StatusPill, HeaderRight, AppShell
Re-run the grep at execution time — the authoritative target is `grep -r indigo web/src` returning nothing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate indigo→blue across all call sites + remaining primitives</name>
  <files>web/src/** (every file currently containing `indigo`): pages/*, components/*, components/ui/{Toggle,Tabs,StatusPill,HeaderRight,AppShell}.tsx, lib/task-templates.ts</files>
  <action>Find every occurrence with `grep -rn indigo web/src`. For each, replace `indigo` with `blue` preserving the numeric shade and opacity suffix exactly (e.g. `focus:ring-indigo-500` → `focus:ring-blue-500`, `bg-indigo-600/20` → `bg-blue-600/20`, `text-indigo-300` → `text-blue-300`, `ring-indigo-500/30` → `ring-blue-500/30`). This is a pure shade swap — do not alter layout, opacity values, or non-indigo classes. Do NOT touch the six files migrated in 08-01. Per design-prefs, blue is now the sole app accent; orange remains reserved for CTA-only and must not be introduced here. After editing, confirm `grep -r indigo web/src` returns nothing.</action>
  <verify>
    <automated>cd web && ! grep -rq indigo src && echo "zero indigo in web/src"</automated>
  </verify>
  <done>`grep -r indigo web/src` returns no matches; only color shade tokens changed (indigo→blue); no layout/behavior diffs.</done>
</task>

<task type="auto">
  <name>Task 2: CI grep guard test</name>
  <files>web/test/no-indigo.test.ts</files>
  <action>Create a bun test web/test/no-indigo.test.ts that recursively reads every file under web/src (skip binary/asset files) and asserts none contain the substring `indigo` (case-insensitive). On any match, the test must fail and report the offending file path(s) + line(s) so the CI build blocks reintroduction (per R-DS-01 acceptance). Use Bun's built-in test runner + fs (Bun.Glob or fs.readdirSync recursion). Keep it dependency-free. Note in a top-of-file comment that this guard enforces the design-preferences.md "never indigo" accent rule.</action>
  <verify>
    <automated>cd web && bun test test/no-indigo.test.ts</automated>
  </verify>
  <done>Test passes on the migrated tree; flipping any file back to `indigo` makes it fail with the file path reported.</done>
</task>

</tasks>

<verification>
- `cd web && ! grep -rq indigo src` (zero matches).
- `cd web && bun test test/no-indigo.test.ts` passes.
- `bun run build:web` clean.
- `cd hub && bun test` unaffected (no hub files touched).
</verification>

<success_criteria>
Zero `indigo` under web/src; all migrated accents are blue per the locked mapping; the no-indigo guard test passes and would fail on any reintroduction; web build clean; hub tests unaffected.
</success_criteria>

<output>
Create `.planning/phases/08-design-system-foundation/08-02-SUMMARY.md` when done.
</output>
