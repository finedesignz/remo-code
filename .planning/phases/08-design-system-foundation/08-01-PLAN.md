---
phase: 08-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/ui/Button.tsx
  - web/src/components/ui/Card.tsx
  - web/src/components/ui/Field.tsx
  - web/src/components/ui/Brand.tsx
  - web/src/components/ui/InfoTip.tsx
  - web/src/components/ui/index.ts
autonomous: true
requirements: [R-DS-02, R-DS-03, R-DS-04, R-DS-05]
must_haves:
  truths:
    - "InfoTip renders a Lucide info icon with a styled (non-native) tooltip"
    - "Field helper copy renders as a title-row InfoTip, not an inline <p>"
    - "Button md/touch hit area is >=44px tall on mobile"
    - "Standalone Card renders a hairline border + shadow-sm by default; a flat variant exists for table wrappers"
    - "Brand logo anchor has horizontal padding"
  artifacts:
    - path: "web/src/components/ui/InfoTip.tsx"
      provides: "InfoTip tooltip primitive (Lucide info + styled tooltip)"
      exports: ["InfoTip", "InfoTipProps"]
    - path: "web/src/components/ui/index.ts"
      provides: "barrel export including InfoTip"
      contains: "InfoTip"
  key_links:
    - from: "web/src/components/ui/Field.tsx"
      to: "web/src/components/ui/InfoTip.tsx"
      via: "import + render in label row"
      pattern: "InfoTip"
    - from: "web/src/components/ui/index.ts"
      to: "web/src/components/ui/InfoTip.tsx"
      via: "barrel re-export"
      pattern: "export.*InfoTip"
---

<objective>
Build the Phase 08 design-system primitives: a new InfoTip tooltip primitive, 44px Button touch targets, Card hairline border + shadow, Field-helper-as-InfoTip, and logo spacing. Accent on these six files is migrated to blue here too (they are the source-of-truth primitives).

Purpose: every downstream phase consumes these primitives; this is the blocking foundation.
Output: 5 modified primitives + 1 new InfoTip + barrel export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/settings-connections-overhaul/PLAN.md
@.planning/REQUIREMENTS.md

<interfaces>
Current primitive contracts (from codebase — keep APIs additive, no breaking changes):

Button.tsx:
  export type ButtonSize = "sm" | "md";
  SIZE = { sm: "px-3 py-1.5 text-sm", md: "px-3 py-2 text-sm" }   // ~30-32px tall — too small
  primary VARIANT uses bg-indigo-600 hover:bg-indigo-500; focus ring uses focus:ring-indigo-500

Card.tsx:
  props: { padded?: boolean (default true), bg?: "primary"|"secondary" (default secondary) }
  current className: cn("rounded-xl", surface, padded && "p-5", className) — no border, no shadow
  stale doc comment: "no border, no shadow. Contrast IS separation."

Field.tsx:
  props: { label, helper?, error?, children, className? }
  helper currently rendered as <p className="text-xs text-[var(--text-muted)] mt-1">{helper}</p>

Brand.tsx:
  <a className="flex items-center gap-2 rounded-lg focus:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500"> — no horizontal padding

index.ts: barrel exports each primitive + its prop types. Lucide is already a dependency (used elsewhere).
</interfaces>

Accent rule (design-preferences.md, LOCKED): app accent = BLUE. blue-600/500 for solid, blue-500/30 rings, focus ring blue-500, links blue-300/400. Orange = CTA-only. NEVER indigo.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InfoTip primitive + barrel export</name>
  <files>web/src/components/ui/InfoTip.tsx, web/src/components/ui/InfoTip.test.tsx, web/src/components/ui/index.ts</files>
  <behavior>
    - Renders a Lucide `Info` icon button (glyph 14-16px) with a >=44px tap target via padding (e.g. p-2.5/p-3).
    - Shows tooltip content on hover (desktop) / focus / tap (mobile) — a styled positioned element, NOT a native `title=` attribute.
    - Accepts `content: ReactNode` and optional `label`/aria props; icon at text-[var(--text-muted)] rest, blue accent on hover.
    - Exported from ui/index.ts as `InfoTip` + `InfoTipProps`.
  </behavior>
  <action>Create web/src/components/ui/InfoTip.tsx exporting `InfoTip` (per R-DS-03). Use the Lucide `Info` icon. Render a styled tooltip (absolute/portal-positioned div, rounded-lg, bg-[var(--bg-secondary)], soft border, text-xs) revealed on hover+focus and toggled on tap — do NOT use the native `title=` attribute. Keep the glyph at 14-16px but pad the trigger so the hit area is >=44x44px (per design-prefs small-icon-big-hit-area). Accent any hover/focus state with blue (text-blue-400 / focus:ring-blue-500), never indigo. Export `InfoTip` and `InfoTipProps` from web/src/components/ui/index.ts. Reuse cn from ../../lib/ui/cn.</action>
  <verify>
    <automated>cd web && bunx tsc --noEmit 2>&1 | grep -i InfoTip; test $? -ne 0 && echo "no InfoTip type errors"</automated>
  </verify>
  <done>InfoTip.tsx exists, exported from index.ts, no native title= used, tooltip is a styled element, hit area >=44px.</done>
</task>

<task type="auto">
  <name>Task 2: Button 44px sizing + blue accent</name>
  <files>web/src/components/ui/Button.tsx</files>
  <action>In web/src/components/ui/Button.tsx (per R-DS-02): change `md` size to `px-4 py-2.5 text-sm` so the rendered height is >=44px. Keep `sm` as the compact desktop variant but ensure its mobile hit area reaches >=44px (e.g. add `min-h-[44px]` or sufficient py at base, tightening only at `sm:`/`md:` breakpoints). Per the milestone PLAN, you MAY add a `touch` size to `ButtonSize` for explicitly large targets — if added, export the updated `ButtonSize` (already re-exported via index.ts). Migrate accent (per R-DS-01 scope for this file): primary VARIANT `bg-indigo-600 hover:bg-indigo-500` → `bg-blue-600 hover:bg-blue-500`; focus ring `focus:ring-indigo-500` → `focus:ring-blue-500`. No other behavior change.</action>
  <verify>
    <automated>cd web && grep -c indigo src/components/ui/Button.tsx | grep -qx 0 && echo "Button indigo-free"</automated>
  </verify>
  <done>md = px-4 py-2.5 (>=44px); compact sm keeps >=44px mobile hit area; zero indigo in Button.tsx; build types clean.</done>
</task>

<task type="auto">
  <name>Task 3: Card border+shadow, Field→InfoTip, Brand spacing (+ blue accent)</name>
  <files>web/src/components/ui/Card.tsx, web/src/components/ui/Field.tsx, web/src/components/ui/Brand.tsx</files>
  <action>
Card.tsx (R-DS-04): add a hairline border + soft shadow by default for standalone cards — `border border-[var(--border-color)]/40 shadow-sm`. Provide a flat variant for table wrappers: add an optional prop (e.g. `flat?: boolean`, default false) that omits the border+shadow; keep existing `padded`/`bg` props unchanged. Update the stale doc comment ("no border, no shadow. Contrast IS separation.") to describe the new modern-subtle default + flat variant.

Field.tsx (R-DS-03): replace the inline helper `<p className="text-xs text-[var(--text-muted)] mt-1">{helper}</p>` with a title-row InfoTip — render `helper` (when present) as an InfoTip immediately right of the label inside the existing label row. Import InfoTip from "./InfoTip". Keep the `error` <p> branch as-is (errors stay inline, not tooltipped).

Brand.tsx (R-DS-05 + R-DS-01): add horizontal padding to the `<a>` anchor (e.g. `px-3` / `px-4`) so the logo has breathing room left+right. Migrate the focus ring `focus-visible:ring-indigo-500` → `focus-visible:ring-blue-500`.
  </action>
  <verify>
    <automated>cd web && c=$(grep -l indigo src/components/ui/Card.tsx src/components/ui/Field.tsx src/components/ui/Brand.tsx 2>/dev/null | wc -l); test "$c" -eq 0 && grep -q InfoTip src/components/ui/Field.tsx && echo "ok"</automated>
  </verify>
  <done>Card default has border+shadow with a flat variant + updated comment; Field helper renders via InfoTip in the label row; Brand has horizontal padding; all three files indigo-free.</done>
</task>

</tasks>

<verification>
- `cd web && bun run build` (or repo `bun run build:web`) compiles clean.
- `grep -rc indigo web/src/components/ui/` shows 0 across Button, Card, Field, Brand, InfoTip, index.
- InfoTip uses no native `title=`.
</verification>

<success_criteria>
InfoTip primitive exists + exported; Button md>=44px; Card border+shadow default with flat variant; Field helper is an InfoTip; Brand padded; these six files contain zero `indigo`. `bun run build:web` clean.
</success_criteria>

<output>
Create `.planning/phases/08-design-system-foundation/08-01-SUMMARY.md` when done.
</output>
