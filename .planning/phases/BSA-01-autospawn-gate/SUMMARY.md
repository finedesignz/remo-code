# BSA-01-autospawn-gate — SUMMARY

REMO_ORCHESTRATOR_AUTOSPAWN env gate (default OFF, call-time) + isAutospawnEnabled() in controller.ts; carries REMO_ORCHESTRATOR_ENABLED. Inert until wired. Tests: orchestrator-autospawn-gate.test.ts.

All OFF-by-default + inert until owner arms (allowlist + REMO_ORCHESTRATOR_AUTOSPAWN flip). Cost cap untouched; no API key on human PTY path.
