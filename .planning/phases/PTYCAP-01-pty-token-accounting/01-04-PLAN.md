---
phase: PTYCAP-01-pty-token-accounting
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - tools/regression-baseline.json
  - .woodpecker/qc.yaml
  - docs/usage-cost.md
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3]

must_haves:
  truths:
    - "The repo's QC gate actually RUNS the SC-1 proof: bun test supervisor/test/pty-usage-tail.test.ts executes in Woodpecker's PR gate, not only on a developer's laptop."
    - "tools/regression-baseline.json reflects a re-measured pass/skip count with the floor moved deliberately and a written reason, rather than absorbing the phase's new tests into slack."
    - "Every new skip introduced by this phase is REMO_E2E_DB_URL-gated with an explicit stated reason — the only kind of skip this repo sanctions."
    - "docs/usage-cost.md documents the PTY accounting path, the runner_type bucket, and the four planner decisions a future reader would otherwise mistake for oversights (token_usage_daily PK not extended; transcript_path not wired; claude-only scoping; estimated-cost-only)."
    - "CLAUDE.md's Usage cost ledger docs-map row names the PTY source so a future session finds the mechanism from the map alone."
  artifacts:
    - "tools/regression-baseline.json — re-measured counts + a PTYCAP skip/floor note"
    - ".woodpecker/qc.yaml — a step running supervisor/test/pty-usage-tail.test.ts"
    - "docs/usage-cost.md — a PTY token accounting section"
  key_links:
    - "supervisor/test/pty-usage-tail.test.ts (SC-1's authoritative command) → .woodpecker/qc.yaml — without this link the SC-1 proof never runs in CI, because check-baseline covers hub/test ONLY"
    - "docs/usage-cost.md ← the P1-D-C / P1-D-D / P1-D-E deferrals recorded in 01-01-PLAN.md"
---

<objective>
Make the phase's proofs actually gate, move the regression baseline deliberately, and write
the mechanism and its four explicit deferrals down where the next session will find them.

Purpose: `bun run check-baseline` runs `hub/test` ONLY, and `.woodpecker/qc.yaml` contains no
supervisor test step at all — so without this plan, SC-1's authoritative test
(`supervisor/test/pty-usage-tail.test.ts`) would exist and never run in CI. Meanwhile this
phase adds tests and DB-gated skips that the baseline budget must absorb by measurement, not
by luck. And this repo's own rule is that a doc is updated in the SAME commit as the behavior
it describes.

Output: CI wiring, a re-measured baseline with a written reason, and docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/PTYCAP-01-pty-token-accounting/01-01-SUMMARY.md
@.planning/phases/PTYCAP-01-pty-token-accounting/01-02-SUMMARY.md
@.planning/phases/PTYCAP-01-pty-token-accounting/01-03-SUMMARY.md
@CLAUDE.md
</context>

<additional_context>
Full multi-source coverage audit, planner decisions P1-D-A..F, and the spec-less-probe skip
record live in `01-01-PLAN.md` and govern this plan too.

**Woodpecker YAML hazard (project memory, PR #305):** Woodpecker rewrites a `${...}`
sequence ANYWHERE in a `.woodpecker/*.yaml` file — including inside comments — and a
malformed one is a compile error that blocks the ENTIRE pipeline, not just the step. Use
plain literals only in any line added to `qc.yaml`; never write a `${...}` sequence there,
not even illustratively in a comment.

**Docs-sync:** this phase adds no HTTP route and changes no OpenAPI surface, so
`bun run docs:sync` is NOT required and `docs/openapi.json` / `docs/api.md` must not change.
The docs-drift pipeline should stay green untouched.
</additional_context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Make the SC-1 proof gate in CI, and re-measure the regression baseline deliberately</name>

  <files>.woodpecker/qc.yaml (MODIFY), tools/regression-baseline.json (MODIFY)</files>

  <read_first>
- `.woodpecker/qc.yaml` — the whole file (it is short). Note the exact `commands:` list ordering and that every existing entry is a plain literal; note also the header comment explaining the Woodpecker-vs-GHA split.
- `tools/regression-baseline.json` — its `_comment`, `captured_at`, `captured_from`, the successive `_skip_note_*` entries, and the gate block's `_comment` explaining that `pass_min` is a real floor with roughly 1.5% headroom and that `skip_max` sanctions ONLY explicitly `REMO_E2E_DB_URL`-gated skips. This file's own prose defines the convention this task must follow.
- `tools/check-baseline.ts` — specifically the source-tree walk, to confirm for yourself that it collects `hub/test` only and therefore never runs a supervisor test.
- `.planning/phases/PTYCAP-01-pty-token-accounting/01-VALIDATION.md` — the Sampling Rate section (per-task file run, per-wave `check-baseline`, full suite green before verify).
- Project memory `reference_woodpecker_var_parse_gotcha.md` if reachable — otherwise rely on the hazard note in `<additional_context>`.
  </read_first>

  <behavior>
- `.woodpecker/qc.yaml` gains one `commands:` entry that runs the supervisor test file added by plan 01, positioned AFTER `bun install --frozen-lockfile` and — to keep a failure legible — after `bun run check-baseline`. The pipeline still compiles (no dollar-brace sequence anywhere in the diff).
- The added step runs exactly the one deterministic new file, not a wildcard over `supervisor/test/`, so an unrelated pre-existing supervisor test that has never run on linux/amd64 cannot red the PR gate.
- `tools/regression-baseline.json` carries freshly measured actuals from a bare-checkout `bun run check-baseline` run on this branch, a `captured_at` of the measurement date, and a new `_skip_note_ptycap` string that states: the measured pass/skip/fail, which files added passes, which added `REMO_E2E_DB_URL`-gated skips and why each is DB-gated, and whether `pass_min` / `skip_max` moved and by how much.
- `pass_min` is raised if and only if the measured pass count rose enough to keep the file's stated headroom convention; if it does not move, the note says explicitly that it was left and why.
- `skip_max` is raised only by the exact number of new DB-gated skips measured, and the note names them.
- `fail_max` stays 0.
  </behavior>

  <action>
First measure, then edit. Run `bun run check-baseline` from a bare environment on this branch with no `REMO_E2E_DB_URL` and no `DATABASE_URL` exported (that is the FLOOR configuration the file's `captured_from` describes) and record the printed pass / skip / fail / total. Do not guess these numbers and do not copy the previous run's.

Edit `tools/regression-baseline.json`: update `captured_at` to today, extend `captured_from` (or add the new `_skip_note_ptycap` key alongside the existing `_skip_note_once` / `_skip_note_work` keys, matching their style) with the measured figures and a per-file accounting of the deltas — `hub/test/token-usage-runner-type.test.ts`, `hub/test/no-hub-side-transcript-fs.test.ts` and the new cases in `hub/test/usage-event-handler.test.ts` add passes; `hub/test/pty-usage-midflight-visibility.test.ts` and the new case in `hub/test/e2e/schema-double-apply.e2e.test.ts` add `REMO_E2E_DB_URL`-gated skips. State in the note that every added skip is DB-gated with a stated reason and that no unconditional skip was introduced. Then set `pass_min` and `skip_max` per the `<behavior>` rules and leave `fail_max` at 0.

Note explicitly in the same string that `supervisor/test/*` is NOT counted by `check-baseline` (it walks `hub/test` only), which is exactly why the Woodpecker step below exists.

Edit `.woodpecker/qc.yaml`: append one plain-literal command entry running `bun test supervisor/test/pty-usage-tail.test.ts` after the `bun run check-baseline` entry, preceded by a short plain-literal comment saying that `check-baseline` covers `hub/test` only so this SC-1 proof needs its own step. Write nothing containing a dollar sign followed by a brace anywhere in the diff. Do not add a wildcard supervisor test run, do not reorder existing steps, and do not touch the `services:` or `environment:` blocks.
  </action>

  <verify>
    <automated>cd C:/Users/artic/GitHub/remo-code-ptycap && bun run check-baseline</automated>
    <automated>cd C:/Users/artic/GitHub/remo-code-ptycap && bun test supervisor/test/pty-usage-tail.test.ts</automated>
    <automated>cd C:/Users/artic/GitHub/remo-code-ptycap && grep -q 'supervisor/test/pty-usage-tail.test.ts' .woodpecker/qc.yaml && ! grep -qF '${' .woodpecker/qc.yaml && echo QC_YAML_OK</automated>
  </verify>

  <acceptance_criteria>
- `bun run check-baseline` exits 0 against the freshly written `tools/regression-baseline.json` (pass at or above `pass_min`, skip at or below `skip_max`, fail 0).
- `bun test supervisor/test/pty-usage-tail.test.ts` exits 0 — the command the new CI step will run.
- CLI assertion: the third verify command prints `QC_YAML_OK`, proving both that the SC-1 proof is wired into the PR gate AND that no dollar-brace sequence exists anywhere in `.woodpecker/qc.yaml`.
- Source assertion: `tools/regression-baseline.json` contains a `_skip_note_ptycap` (or equivalently named) string that states the measured pass/skip/fail figures, names each file that added passes, names each file that added a `REMO_E2E_DB_URL`-gated skip, and states whether `pass_min` and `skip_max` moved and why.
- Source assertion: `tools/regression-baseline.json` still has `"fail_max": 0`.
- Source assertion: `git diff .woodpecker/qc.yaml` adds exactly one command entry plus at most one comment line, and touches neither `services:` nor `environment:`.
- Behavior assertion: the added CI step names one specific test file, not a wildcard over `supervisor/test/`.
  </acceptance_criteria>

  <done>
SC-1's authoritative test runs in the Woodpecker PR gate; the regression baseline carries
re-measured actuals with a written, per-file reason for every moved bound; no unconditional
skip was introduced; and the pipeline still compiles.
  </done>

  <precondition>
`bun install --frozen-lockfile` has been run in this worktree, and neither `REMO_E2E_DB_URL`
nor `DATABASE_URL` is exported in the measuring shell — `tools/regression-baseline.json`'s
`captured_from` defines the FLOOR configuration as a bare checkout with no ambient env and
no database, and measuring with a DB present would record inflated pass counts and deflated
skip counts that a bare CI checkout could never reproduce.
  </precondition>

  <reversibility rating="reversible">
Both edits are single-line/single-key config changes; reverting either restores the prior
gate exactly. The `pass_min` / `skip_max` figures are re-measurable at any time.
  </reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document the PTY accounting path and its four explicit deferrals</name>

  <files>docs/usage-cost.md (MODIFY), CLAUDE.md (MODIFY)</files>

  <read_first>
- `docs/usage-cost.md` — the whole file. Its existing diagram (the `agent→hub WS usage_event` line around line 18), its "P2 records only / P3 owns the cap" framing around lines 33 and 77, and its heading style are what the new section must slot into rather than duplicate.
- `CLAUDE.md` — the "Usage cost ledger" row of the Docs map table, and the "Cost cap is non-bypassable" cross-cutting invariant paragraph (which already describes `token_usage` as the single source and names the companion daily TOKEN cap).
- `.planning/phases/PTYCAP-01-pty-token-accounting/01-01-PLAN.md` — the `<additional_context>` block listing planner decisions P1-D-A through P1-D-F; the deferrals documented here are P1-D-C, P1-D-D, P1-D-E and P1-D-F.
- `.planning/phases/PTYCAP-01-pty-token-accounting/01-01-SUMMARY.md`, `01-02-SUMMARY.md`, `01-03-SUMMARY.md` — the as-built shape, in case an executor deviated from plan text; document what was BUILT, not what was planned.
- `.planning/phases/PTYCAP-01-pty-token-accounting/01-RESEARCH.md` — Assumption A2, which asks for one live smoke test (spawn a real PTY session through this branch's supervisor, confirm the JSONL appears and updates) before trusting this in production. That request is recorded as a documented manual verification item, not silently dropped.
  </read_first>

  <behavior>
`docs/usage-cost.md` gains a section covering:
- The PTY accounting path end to end: the interactive CLI writes its own per-project transcript JSONL on the SUPERVISOR host; the supervisor tails it per live session; each assistant record carrying a usage block becomes a `usage_event` tagged `runner_type: 'pty-interactive'`; the hub persists it into `token_usage.runner_type`.
- Why the tail lives supervisor-side and not hub-side (the hub container has no such path — the same reason `REMO_TELEGRAM_TRANSCRIPT_TAIL` is documented as keep-OFF in Coolify), with a pointer to the guard test that enforces it.
- The two-value `runner_type` domain and its live CHECK constraint.
- Four explicitly-recorded decisions, each stated as a decision with its reason so a future reader does not read them as bugs: `token_usage_daily`'s PK is NOT split by runner_type (P1-D-D); `sessions.transcript_path` / `setSessionPtyIdentity()` remain unwired (P1-D-C); accounting is claude-only, with Codex named as a fast-follow blocked on verifying the Codex rollout JSONL carries an equivalent per-turn usage field (P1-D-E); PTY-sourced cost is always the hub's list-price ESTIMATE because the transcript carries no per-record cost (P1-D-F).
- A note that this is RECORD-only and that gating the PTY path is Phase 2 of milestone PTYCAP.
- A short manual-verification item preserving 01-RESEARCH.md Assumption A2: spawn one real PTY session through the supervisor on a live host and confirm the transcript JSONL appears and its usage records reach `token_usage` with the interactive tag. Mark it as an operator smoke check, not a CI gate.

`CLAUDE.md`'s Docs map "Usage cost ledger" row is extended so it names the PTY source
alongside the existing stream-json source, and states that `runner_type` splits the two buckets.
  </behavior>

  <action>
Append a clearly-headed section to `docs/usage-cost.md` — do not rewrite the existing P2/P3
content and do not restate the cost-cap invariant that already lives in `CLAUDE.md`. Match
the file's existing heading depth and its plain-ASCII diagram style; a short arrow diagram
for the PTY path parallel to the existing stream-json one is the clearest form. Reference
concrete symbols by name so the doc stays greppable: `supervisor/src/usage/pty-usage-emitter.ts`,
`supervisor/src/usage/pty-transcript-tail.ts`, `token_usage.runner_type`,
`token_usage_runner_type_check`, `idx_token_usage_user_runner_type`,
`hub/test/no-hub-side-transcript-fs.test.ts`, `supervisor/test/pty-usage-tail.test.ts`.

Write the four decisions as a short labelled list, each one sentence of decision plus one
sentence of reason, so the reason survives without the plan file.

Then extend the `CLAUDE.md` Docs map "Usage cost ledger" row in place — a phrase, not a
rewrite — naming the PTY transcript-tail source and the `runner_type` bucket split, and
keeping the row's existing supervisor-version note intact. Do not restructure the table, do
not touch any other row, and do not add a new top-level section to `CLAUDE.md`.

Do not run `bun run docs:sync` and do not modify `docs/openapi.json` or `docs/api.md` — this
phase adds no HTTP route, so there is no OpenAPI drift to regenerate and regenerating would
produce noise the docs-drift pipeline has to review.
  </action>

  <verify>
    <automated>cd C:/Users/artic/GitHub/remo-code-ptycap && grep -q 'pty-interactive' docs/usage-cost.md && grep -q 'pty-usage-emitter' docs/usage-cost.md && grep -q 'runner_type' CLAUDE.md && git diff --name-only -- docs/openapi.json docs/api.md | grep -q . && echo UNEXPECTED_OPENAPI_DRIFT || echo DOCS_OK</automated>
    <automated>cd C:/Users/artic/GitHub/remo-code-ptycap && bun run check-baseline</automated>
  </verify>

  <acceptance_criteria>
- CLI assertion: the first verify command prints `DOCS_OK` — `docs/usage-cost.md` mentions both `pty-interactive` and `pty-usage-emitter`, `CLAUDE.md` mentions `runner_type`, and neither `docs/openapi.json` nor `docs/api.md` changed.
- `bun run check-baseline` still exits 0 (documentation edits must not disturb the gate).
- Source assertion: `docs/usage-cost.md` contains all four decision labels' subjects — a statement that `token_usage_daily`'s primary key is unchanged, a statement that `transcript_path` remains unwired, a statement that accounting is claude-only with Codex as a fast-follow, and a statement that PTY cost is an estimate.
- Source assertion: `docs/usage-cost.md` contains the operator smoke-check item preserving 01-RESEARCH.md Assumption A2.
- Source assertion: `git diff CLAUDE.md` touches only the "Usage cost ledger" Docs map row.
- Source assertion: `git diff --stat` for this task lists exactly two files.
  </acceptance_criteria>

  <done>
A future session reading `docs/usage-cost.md` learns how PTY spend reaches the ledger, why
the tail is supervisor-side, what `runner_type` means, which four things were deliberately
NOT done and why, and what one manual smoke check remains — without opening a single
planning file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer laptop → CI gate | The only boundary this plan touches. A proof that runs only locally does not gate; wiring it into `.woodpecker/qc.yaml` moves the SC-1 assertion across that boundary. |
| CI config parser | Woodpecker compiles the whole `.woodpecker/*.yaml` file before running any step; a malformed variable sequence anywhere in the file — comments included — blocks the entire pipeline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-11 | Repudiation (an assertion that never runs) | `supervisor/test/pty-usage-tail.test.ts` vs the PR gate | high | mitigate | `check-baseline` walks `hub/test` ONLY, so SC-1's authoritative proof would exist and never execute in CI — a green PR gate would be claiming coverage it does not have. Task 1 adds an explicit `.woodpecker/qc.yaml` step and the third verify command fails if that step is missing. |
| T-1-12 | Denial of Service (whole CI pipeline) | `.woodpecker/qc.yaml` | high | mitigate | Woodpecker rewrites a dollar-brace sequence anywhere in the file, including comments, and a malformed one is a compile error that blocks every step (PR #305). Task 1 forbids the sequence in the diff and the third verify command greps the whole file for it. |
| T-1-13 | Repudiation (baseline ratchet) | `tools/regression-baseline.json` | medium | mitigate | Absorbing new tests into existing slack, or raising `skip_max` without naming what was skipped, converts a gate into a ratchet — the exact failure the file's own gate `_comment` warns about. Task 1 requires a fresh FLOOR-configuration measurement and a per-file written reason, and pins `fail_max` at 0. |
| T-1-14 | Repudiation (undocumented deferral read as a bug) | `docs/usage-cost.md` | medium | mitigate | Four decisions (unsplit daily PK, unwired `transcript_path`, claude-only scoping, estimated-only cost) look identical to oversights from the outside. Task 2 records each as a decision plus its reason in the doc that outlives the plan files, and preserves the one remaining manual smoke check from 01-RESEARCH.md Assumption A2. |
| T-1-06 | Elevation of Privilege | new gate/dispatch surface | n/a | accept (no surface) | This plan modifies only CI config, a baseline JSON, and two markdown files. No production code, no gate, no dispatch call site. |
| T-1-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | accept (no surface) | Zero package-manager install tasks in this plan. The added CI step invokes `bun test` on an existing file; no new image, no new dependency, no lockfile change. |
</threat_model>

<artifacts_this_phase_produces>
Symbols and paths CREATED or EXTENDED by this plan (newly-created — not pre-existing drift):

**New config/data keys**
- a `_skip_note_ptycap` (or equivalently named) key in `tools/regression-baseline.json`
- one new `commands:` entry in `.woodpecker/qc.yaml` running `bun test supervisor/test/pty-usage-tail.test.ts`

**New documentation sections**
- a PTY token accounting section in `docs/usage-cost.md`
- an extended "Usage cost ledger" Docs map row in `CLAUDE.md`

No new production symbols, no new schema objects, no new WS fields. All production symbols
this plan documents are listed in `01-01-PLAN.md`'s "Artifacts this phase produces" section.
</artifacts_this_phase_produces>

<verification>
- `bun run check-baseline` — green against the re-measured `tools/regression-baseline.json`.
- `bun test supervisor/test/pty-usage-tail.test.ts` — green (the command the new CI step runs).
- The `.woodpecker/qc.yaml` assertion prints `QC_YAML_OK`.
- The docs assertion prints `DOCS_OK`, with `docs/openapi.json` and `docs/api.md` unchanged.
- `git diff --name-only` for this plan lists exactly: `tools/regression-baseline.json`,
  `.woodpecker/qc.yaml`, `docs/usage-cost.md`, `CLAUDE.md`.
</verification>

<success_criteria>
- SC-1's authoritative test executes in the Woodpecker PR gate.
- The regression baseline reflects a fresh FLOOR-configuration measurement, with every moved
  bound justified per file and every added skip explicitly DB-gated with a reason.
- `fail_max` remains 0 and no unconditional skip was introduced.
- The PTY accounting path, the `runner_type` bucket, the four deferrals and the one remaining
  operator smoke check are documented in `docs/usage-cost.md`, with `CLAUDE.md`'s Docs map row
  pointing at it.
- No production code, no gate, and no OpenAPI surface changed.
</success_criteria>

<output>
Create `.planning/phases/PTYCAP-01-pty-token-accounting/01-04-SUMMARY.md` when done.
</output>