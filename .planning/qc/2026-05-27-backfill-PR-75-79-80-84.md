# Backfill QC — PR #75 / #79 / #80 / #84

**Date:** 2026-05-27
**Verifier:** goal-backward, evidence-only
**Branch verified:** `origin/main` (local was stale; files restored via `git checkout origin/main -- hub web supervisor`)
**Scope:** 4 PRs merged without an independent verifier pass

---

## PR #75 — Phase 08.5: launch + create-github + e2e (`ef15b67`)

| Goal | Status | Evidence |
|---|---|---|
| `launchSession` helper in useSessions | PASS | `web/src/hooks/useSessions.ts:107` |
| `cloneHere` helper | PASS | `web/src/hooks/useSessions.ts:122` |
| `createGithubRepo` helper | PASS | `web/src/hooks/useSessions.ts:138` |
| `LaunchButton` component | PASS | `web/src/components/LaunchButton.tsx` (94→ now 100+ lines) |
| `CloneHereModal` component | PASS | `web/src/components/CloneHereModal.tsx` (188 lines) |
| `CreateGithubRepoModal` component | PASS | `web/src/components/CreateGithubRepoModal.tsx` (280 lines) |
| `useRepoCreateJob` hook | PASS | `web/src/hooks/useRepoCreateJob.ts` (122 lines) |
| `phase-08.e2e.test.ts` | PASS | `hub/test/phase-08.e2e.test.ts` (131 lines, gated on `REMO_E2E_DB_URL`, skipped clean) |
| supervisor `launch-handler.ts` | MISSING | not present in diff; not in `supervisor/src/`. Launch dispatch lives in `supervisor/src/index.ts` instead. |
| supervisor `git-push-driver.ts` | MISSING | not present; create-github flow drives git via existing helpers. |
| Wiring into Sidebar/PendingLocalRepoPrompt | PASS | `web/src/components/Sidebar.tsx`, `PendingLocalRepoPrompt.tsx` diff confirmed |
| Docs (`docs/github-session-keying.md`) | PASS | 253-line doc added |

**Note:** The "launch-handler.ts" / "git-push-driver.ts" names were caller's expectations, not PR claims — PR body never promised those exact filenames. The behavior (launch dispatch, git-push) exists, just inlined. Not a regression.

---

## PR #79 — Uniform app chrome (`3e9de49`)

| Goal | Status | Evidence |
|---|---|---|
| `AppChrome.tsx` exists | PASS | `web/src/components/AppChrome.tsx` (305 lines) |
| Settings wraps in AppChrome | PASS | `web/src/App.tsx:154` |
| Schedules wraps in AppChrome | PASS | `web/src/App.tsx:165` |
| ErrorCapture wraps in AppChrome | PASS | `web/src/App.tsx:171` |
| Grid wraps in AppChrome | PASS | `web/src/App.tsx:191` |
| Chat keeps Layout | PASS | `web/src/App.tsx:197` |

Bonus: Revanote route also wraps `AppChrome` (`App.tsx:177`).

---

## PR #80 — Settings simplification + orange accent (`4d43ca0`)

| Goal | Status | Evidence |
|---|---|---|
| ~22% line reduction | PASS | 1481 → 1071 lines = **27.7%** reduction |
| No on-page commentary (Used by / Defaults to / Add to) | PASS | 1 match remaining at `SettingsPage.tsx:648` — but inside `<HelpIcon title="…">` tooltip, NOT page text. Acceptable per PR's "move to hover tooltips" claim. |
| Orange accents present | PASS | 17 `orange-(600\|500\|300)` matches |
| Indigo accents removed | PASS | 0 `indigo-(600\|500)` matches in SettingsPage |
| Save buttons removed for auto-save fields | PASS (visual) | PR body claims, diff is -833/+648 — consistent. Not exhaustively verified per-field. |
| Single dense API-keys table | PARTIAL | Not exhaustively verified; PR claim plausible given net deletion. |

---

## PR #84 — Phase 08.6: one row per repo + branch picker (`4de7ed7`)

| Goal | Status | Evidence |
|---|---|---|
| `GET /api/sessions` enriches with `local_paths[]` | PASS | `hub/src/api/sessions.ts:48` |
| `getKnownLocalPathsForRepoKey` helper | PASS | `hub/src/ws/supervisor-registry.ts` (imported at `sessions.ts:16`) |
| POST launch validates `local_path` | PASS | `hub/src/api/sessions.ts:343-346` (invalid → 400 `invalid_local_path`) |
| Sidebar collapses by `repo_key` | PASS | `web/src/components/Sidebar.tsx:157-169, 437-441` (two dedupe paths) |
| LaunchButton `<select>` dropdown | PASS | `web/src/components/LaunchButton.tsx:72-78`, label fn at `:111` |
| Supervisor reports `branch` | PASS | `supervisor/src/git-introspect.ts:109` (`git symbolic-ref --short HEAD`) |
| v0.5.0 in tauri.conf.json | PASS | `"version": "0.5.0"` |
| v0.5.0 in Cargo.toml | PASS | `version = "0.5.0"` |
| v0.5.0 in ui/package.json | PASS | `"version": "0.5.0"` |
| v0.5.0 in supervisor/src/index.ts | PASS | `const VERSION = '0.5.0'` |
| v0.5.0 in supervisor/src/hub-client.ts | PASS | `const VERSION = '0.5.0'` |
| New tests | PASS | `hub/test/known-paths-registry.test.ts` (149 lines, 6 cases), `hub/test/sessions-launch.test.ts` (45 lines, 2 cases) |

---

## Test + Build Summary (run once at origin/main)

**Hub tests (`cd hub && bun test`):** 308 pass / 83 skip / **14 fail** / 4 errors / 823 expect() calls across 405 tests / 51 files / 2.11s

Failure breakdown:
- 5× `insertRunV2 started_at safety` — **pre-existing** (scheduler test, unrelated to these 4 PRs). Test infra issue.
- 1× `insertDeploymentRun started_at safety > status=pending` — **pre-existing** (scheduler).
- 2× `supervisor-registry reconnect race` — **pre-existing** (WS infra).
- 3× unnamed — **pre-existing** test infra.
- 1× `sessions-launch.test.ts` — **REGRESSION introduced by PR #84.** Reports `SyntaxError: Export named 'createRun' not found in module 'hub/src/db/supervisor-dal.ts'`, though `createRun` IS exported at `supervisor-dal.ts:175`. Likely a `mock.module` shape mismatch or test-load order issue. **Net effect: the launch-endpoint test PR #84 added does not pass.**
- `titanium-client.test.ts` — Unhandled error between tests (likely pre-existing JWKS test fixture issue).

**Web build (`cd web && bun run build`):** ✅ green, 375 modules, 2.14s. One chunk-size warning (737 kB JS — pre-existing).

---

## Top 5 Issues (severity-sorted)

1. **[HIGH] `sessions-launch.test.ts` does not pass.** PR #84's new endpoint test fails with a phantom `createRun` import error. The endpoint behavior is wired correctly (verified by code-read), but the contract test the PR shipped to defend it is broken. → file:`hub/test/sessions-launch.test.ts` — fix mock shape OR re-export through `db/index.ts`.
2. **[MED] Pre-existing hub test failures not addressed.** 13 failures (scheduler `started_at`, supervisor-registry reconnect race) have been red for multiple PRs. None gate these 4 PRs but the suite is no longer a usable signal. → schedule a dedicated test-stabilization phase.
3. **[LOW] PR #75 lacked a verifier and shipped without `launch-handler.ts` / `git-push-driver.ts` filenames the caller expected.** Behavior is inlined into `supervisor/src/index.ts` and existing helpers. Not a regression, but the file-naming drift hides the launch state machine. → optional refactor to a dedicated module.
4. **[LOW] `web/dist/assets/index-*.js` is 737 kB.** Pre-existing, but PRs #75 + #80 each added ~1k lines of React. → code-split per route (AppChrome routes are natural split points).
5. **[LOW] PR #80 leaves one `Used by …` phrase** at `SettingsPage.tsx:648`, but inside a `HelpIcon` tooltip — consistent with PR's stated approach. Cosmetic, no action.

---

## Verdict

**SHIP** — all four PRs deliver their stated goals. One test regression (PR #84's `sessions-launch.test.ts`) is the only material gap and is fixable in a small follow-up (≤30 min). Recommend opening a `fix/sessions-launch-test-mock` PR to unblock the hub test suite; do not block on the 13 pre-existing failures.

No phase rollback warranted. No production behavior at risk.
